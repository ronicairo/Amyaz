<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>Votre demande de traduction a été traitée</title>
		<style>
			body {
				font-family: Arial, sans-serif;
				margin: 0;
				padding: 20px;
				background-color: #f6f6f6;
			}
			.email-container {
				max-width: 600px;
				margin: 0 auto;
				background-color: #ffffff;
				padding: 20px;
				border: 1px solid #e9e9e9;
				border-radius: 10px;
			}
			.email-header {
				text-align: center;
				margin-bottom: 20px;
			}
			.email-header img {
				max-width: 150px;
			}
			.email-body {
				font-size: 16px;
				line-height: 1.6;
				text-align: left;
			}
			.email-footer {
				margin-top: 20px;
				font-size: 14px;
				color: #999999;
				text-align: center;
				border-top: 1px solid #e0e0e0;
				padding-top: 20px;
			}
			.word-item {
				background: #eeeeee;
				border: 1px solid #dddddd;
				margin-bottom: 10px;
				padding: 10px;
				border-radius: 5px;
			}
		</style>
	</head>
	<body>
		<div class="email-container">
			<div class="email-header">
				<img src="cid:logo_amyaz" alt="Logo Amyaz" width="60" height="50">
			</div>
			<div class="email-body">
				<p>{{ 'p.hello'|trans }}
					{{ user.username }},</p>
				<p>Votre demande de traduction a été traitée.</p>
				<p>Nouveau statut:
					{% if newStatus == 'decline' %}
						{{ 'post.decline'|trans }}
					{% elseif newStatus == 'validate' %}
						{{ 'post.validate'|trans }}
					{% elseif newStatus == 'change' %}
						{{ 'post.change'|trans }}
					{% elseif newStatus == 'pending' %}
						{{ 'post.pending'|trans }}
					{% else %}
						{{ newStatus }}
					{% endif %}
				</p>
                {% if traduction.justification is not null %}
					<p>Motif:
						{{ traduction.justification }}</p>
				{% endif %}
				<br>
				{% if newStatus == 'change' or newStatus == 'validate' %}
                  <p>Votre demande a été ajoutée au dictionnaire</p>
					<div class="word-item">
						<p>
							<strong>{{ 'post.french_word'|trans }}:
								{{ traduction.wordFR }}</strong>
						</p>
						<p>
							<strong>{{ 'post.english_word'|trans }}:
								{{ traduction.wordEN }}</strong>
						</p>
						<p>
							<em>{{ 'post.rif_singular'|trans }}:</em>
							{{ traduction.singular }}</p>
						<p>
							<em>{{ 'post.rif_plural'|trans }}:</em>
							{{ traduction.plural }}</p>
						<p>
							<em>{{ 'post.phonetic_singular'|trans }}:</em>
							{{ traduction.phoneticSingular }}</p>
						<p>
							<em>{{ 'post.phonetic_plural'|trans }}:</em>
							{{ traduction.phoneticPlural }}</p>
					</div>
				{% endif %}

				<p>{{ 'p.thanks'|trans }}</p>
				<p>Amyaz</p>
			</div>
			<div class="email-footer">
				<p>&copy;
					{{ "now"|date("Y") }}
					{{ 'p.footer_droit'|trans }}
				</p>
			</div>
		</div>
	</body>
</html>
