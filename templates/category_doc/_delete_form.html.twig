<form method="post" action="{{ path('category_doc_delete', {'id': category_doc.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_doc.id) }}">
    <div class="text-center">
    <button class="btn mt-2" style="color: rgb(251, 77, 77);">{{ 'button.delete'|trans }}</button>
    </div>
</form>
